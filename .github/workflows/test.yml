name: Test Workflow

on:
  push:
    branches: [ main, master ]
    workflow_dispatch:

jobs:
  premier-pas:
    runs-on: ubuntu-latest
    steps:
      - name: Récupérer le code
        uses: actions/checkout@v3

      - name: Salutations
        run: echo "Hello Github Actions!"

      - name: Listes des fichiers du projet
        run: ls -R